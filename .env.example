# Copie para .env (que está no .gitignore) só para rodar "netlify dev" local.
# Em produção, o token entra em:
#   Netlify > Site configuration > Environment variables > NOTION_TOKEN
# Nunca comite este valor.
NOTION_TOKEN=ntn_cole_o_token_da_sua_integracao_aqui
